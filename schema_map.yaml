well_logs:
  # Depth columns
  DEPTH:
    name: DEPT
    scale: 1.0
    offset: 0.0
  depth_m:
    name: DEPT
    scale: 1.0
    offset: 0.0
  dept:
    name: DEPT
    scale: 1.0
    offset: 0.0

  # Gamma-ray curves
  "GR (API)":
    name: GR
    scale: 1.0
    offset: 0.0
  GammaRay:
    name: GR
    scale: 1.0
    offset: 0.0

  # Bulk density / RHOB
  RHOB:
    name: RHOB
    scale: 1.0
    offset: 0.0
  Density:
    name: RHOB
    scale: 1000.0
    offset: 0.0

  # Add further mappings as needed...
